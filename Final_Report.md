
# Final Report: Fraud Detection Analysis

## 1. Project Objectives

# Fraud Detection Pipeline: Analysis and Comparison Report

## 1. Executive Summary

This report details the end-to-end development of a machine learning pipeline for fraud detection. The project involved synthesizing a realistic financial transaction dataset, engineering a diverse set of features using both programmatic methods and a Large Language Model (LLM), and evaluating a suite of six different classification models to identify the most effective solution.

The key finding is that gradient boosting models, specifically **XGBoost**, delivered the highest performance, achieving an F1-score of ~0.78 and a ROC AUC of ~0.97. This indicates a superior balance between correctly identifying fraudulent transactions (recall) and minimizing false alarms (precision). LLM-generated features, such as a `consistency_score` between transaction descriptions and merchant categories, proved to be highly predictive.

The final recommendation is the deployment of the XGBoost model due to its state-of-the-art performance. For cases requiring higher interpretability or a wider net for catching fraud at the cost of more false positives, the Random Forest or Logistic Regression models, respectively, serve as viable alternatives.

## 2. Project Objectives

The primary objectives of this project were to:
1.  **Synthesize a High-Quality Dataset:** Create a realistic, labeled dataset for financial fraud detection using the Gemini LLM to generate nuanced transaction details.
2.  **Engineer Advanced Features:** Enhance the dataset by creating both standard programmatic features (e.g., transaction frequency) and novel semantic features using an LLM (e.g., consistency scores, entity extraction).
3.  **Train and Evaluate Multiple Models:** Implement, train, and evaluate a diverse set of six machine learning models to establish a performance baseline and identify the most suitable algorithm for the task. The models evaluated were:
    *   Decision Tree
    *   Random Forest
    *   Logistic Regression
    *   K-Nearest Neighbors (KNN)
    *   XGBoost
    *   Neural Network (MLP)
4.  **Compare and Recommend:** Consolidate the performance metrics of all models into a single comparison file and provide actionable business insights and a final recommendation.
5.  **Document the Process:** Create a comprehensive report detailing every stage of the project, from data creation to final insights, including a full disclosure of AI usage.

## 3. Dataset Details

The project utilized a fully synthetic dataset, created and processed through a multi-stage pipeline.

*   **Raw Data Generation (`simple_llm_fraud_dataset.csv`):** The initial raw dataset of 2,619 transactions was generated by `llm_simple_dataset_simulation.py`. The Gemini API was prompted to create realistic but statistically-controlled fraudulent and legitimate transaction scenarios, including plausible descriptions, amounts, and categorical data. This approach ensured the presence of subtle, non-linear patterns for the models to learn.

*   **Feature-Engineered Dataset (`simple_llm_fraud_dataset_features.csv`):** The raw data was then enriched by `llm_feature_engineering.py`, which added several new programmatic and LLM-generated features.

*   **Final ML-Ready Dataset (`final_ml_ready_dataset.csv`):** The final step, handled by `final_cleaning_and_preparation.py`, involved cleaning the data (removing nulls) and converting all features into a purely numerical format via one-hot encoding for categorical data and TF-IDF for text.

## 4. Exploratory Data Analysis (EDA)

A brief analysis of the final, ML-ready dataset reveals the following key characteristics:

*   **Dataset Shape:** The final dataset contains **2,619 rows** and **160 columns** after all preprocessing. The high number of columns is a direct result of one-hot encoding categorical features and applying TF-IDF to the transaction descriptions.
*   **Fraud Rate:** The dataset has a fraud rate of approximately **8.13%**, which is a common level of class imbalance in fraud detection scenarios.
*   **Key Feature Statistics:**
    *   Average Transaction Amount: $184.28
    *   Average Description Length: 49 characters
    *   Average Transaction Frequency per Customer: 13.1 transactions
    *   LLM-generated Consistency Score (Avg): 4.96 (on a scale of 1-10)

## 5. Feature Engineering

The project employed a hybrid approach to feature engineering, combining standard techniques with novel LLM-based methods.

#### Programmatic Features (No LLM)
These features were created using the `pandas` library in the `llm_feature_engineering.py` and `final_cleaning_and_preparation.py` scripts:
*   **`description_length`**: The character length of the transaction description.
*   **`transaction_frequency`**: The total number of transactions associated with a given `customer_id`.
*   **`hour_of_day` & `day_of_week`**: Extracted from the `timestamp` to capture temporal patterns.

#### LLM-Generated Features
These advanced semantic features were generated by the `llm_feature_engineering.py` script using the Gemini API:
*   **`consistency_score`**: An integer score from 1-10 representing how semantically aligned the `transaction_description` is with the `merchant_category`. A low score indicates a potential anomaly.
*   **`entity_has_*` flags**: A set of binary flags indicating the presence of specific keywords or entities (e.g., `crypto`, `gift_cards`, `refunds`) in the transaction description.

## 6. Modelling & Results

Six different classification models were trained and evaluated on the final dataset. All results were consolidated into `results/model_comparison.csv`.

| model                 | description                                                                                                    | accuracy   | precision  | recall     | f1_score   | roc_auc    |
|-----------------------|----------------------------------------------------------------------------------------------------------------|------------|------------|------------|------------|------------|
| Decision Tree         | A standard Decision Tree classifier. Hyperparameters: {'random_state': 42, 'class_weight': 'balanced', 'max_depth': 10} | 0.963104   | 0.781818   | 0.716667   | 0.747826   | 0.846912   |
| K-Nearest Neighbors   | A non-parametric method using distance to classify. Hyperparameters: {'n_neighbors': 5}                        | 0.956743   | 1.000000   | 0.433333   | 0.604651   | 0.854132   |
| Logistic Regression   | A linear model for binary classification. Includes StandardScaler. Hyperparameters: {...}                        | 0.952926   | 0.676923   | 0.733333   | 0.704000   | 0.887029   |
| Neural Network (MLP)  | A Multi-Layer Perceptron classifier. Hyperparameters: {'hidden_layer_sizes': (100, 50), ...}                   | 0.966921   | 0.904762   | 0.633333   | 0.745098   | 0.950436   |
| Random Forest         | An ensemble of Decision Trees. Hyperparameters: {'n_estimators': 100, ...}                                     | 0.968193   | 0.888888   | 0.666667   | 0.761905   | 0.963866   |
| XGBoost               | A gradient boosting framework. Hyperparameters: {'scale_pos_weight': 12.09, ...}                               | 0.969466   | 0.875000   | 0.700000   | 0.777778   | 0.965657   |

**Analysis:**
The results clearly indicate that the ensemble and gradient boosting methods (`Random Forest`, `XGBoost`) and the `Neural Network` are the top performers, particularly when measured by ROC AUC and F1-score. `XGBoost` achieves the highest F1-score, making it the best-balanced model, while `Random Forest` and the `Neural Network` also show excellent discriminative power. The `K-Nearest Neighbors` model's perfect precision is noteworthy but its low recall makes it impractical as a standalone solution.

## 7. Business Insights & Recommendations

Based on the model comparison, several key insights emerge:

*   **High-Performance Models Deliver Best ROI:** The superior performance of XGBoost and Random Forest justifies their use in a production environment. Their ability to balance precision and recall effectively means they can significantly reduce fraud losses without overwhelming fraud investigation teams with false positives.
*   **Precision vs. Recall Strategy:** The choice of model can be tailored to business needs.
    *   For a **"zero false positives"** strategy where any flagged transaction is almost certainly fraudulent, the KNN model could be used to trigger automated blocking rules, though it would miss many fraud cases.
    *   For a **"catch as much fraud as possible"** strategy, the Logistic Regression model, with its high recall, could serve as an initial filter to cast a wide net, even if it means more manual review.
*   **Feature Importance:** The high performance of all models indicates that the engineered features, particularly the LLM-generated `consistency_score` and entity flags, are providing strong predictive value. This validates the investment in advanced feature engineering.

**Final Recommendation:**

Deploy the **XGBoost model** as the primary fraud detection engine. It offers the best balance of performance (highest F1-score) and discriminatory power (highest ROC AUC), ensuring maximum fraud capture with an efficient rate of false positives. Its complexity is warranted by its state-of-the-art results on this complex, feature-rich dataset.

## 8. AI Usage Disclosure

In the interest of transparency, this project utilized the Google Gemini API in two key areas:

1.  **Synthetic Data Generation:** The initial raw dataset (`simple_llm_fraud_dataset.csv`) was entirely generated by prompting the `gemini-2.5-flash` model. The model was instructed to create a series of realistic fraudulent and legitimate transactions based on a set of probabilistic rules to ensure the data was realistic and contained non-linear patterns.

2.  **Semantic Feature Engineering:** The Gemini API (`gemini-2.5-flash` via `llm_feature_engineering.py`) was used to create advanced features from the raw data. This involved two tasks:
    *   Generating a **`consistency_score`** by evaluating the semantic relationship between a transaction's description and its merchant category.
    *   Extracting **entity flags** by analyzing the transaction description for specific keywords and patterns (e.g., 'crypto', 'gift card').

These steps were crucial for creating a high-quality dataset and a set of powerful predictive features that would be difficult to engineer using purely programmatic methods.


---

## 2. Dataset Details

### 2.1. Original Dataset
The initial dataset is sourced from `data/raw/simple_llm_fraud_dataset.csv`. It contains simulated transactional data with text descriptions.

### 2.2. Schema
The following schema describes the features available in the final machine-learning-ready dataset:

```
# Data Dictionary for Final ML-Ready Dataset (`final_ml_ready_dataset.csv`)

This document describes the schema for the final, ML-ready dataset. All features have been cleaned and converted to a purely numerical format.

## A. Target Variable

| Feature Name | Description                                                                 | Data Type |
| :----------- | :-------------------------------------------------------------------------- | :-------- |
| `is_fraud`   | Binary target variable: `1` if the transaction is fraudulent, `0` otherwise. | Integer   |

## B. Core Numerical Features

These features were either part of the original simulated data or were created programmatically.

| Feature Name                     | Description                                                                  | Data Type |
| :------------------------------- | :--------------------------------------------------------------------------- | :-------- |
| `transaction_amount`             | The monetary value of the transaction.                                       | Float     |
| `description_length`             | The character length of the original transaction description.                | Integer   |
| `transaction_frequency`          | The total number of transactions associated with the customer ID.            | Integer   |
| `hour_of_day`                    | The hour of the day (0-23) when the transaction occurred.                    | Integer   |
| `day_of_week`                    | The day of the week (0=Monday, 6=Sunday) when the transaction occurred.      | Integer   |

## C. LLM-Generated Numerical Features

These features were generated by the Gemini API based on semantic analysis.

| Feature Name                     | Description                                                                  | Data Type |
| :------------------------------- | :--------------------------------------------------------------------------- | :-------- |
| `consistency_score`              | An LLM-generated score (1-10) of how well the description aligns with the category. | Integer   |
| `entity_has_payment_instruments` | Binary flag (`1` or `0`) if the description mentions payment instruments.     | Integer   |
| `entity_has_gift_cards`          | Binary flag (`1` or `0`) if the description mentions gift cards.                | Integer   |
| `entity_has_crypto`              | Binary flag (`1` or `0`) if the description mentions cryptocurrency.          | Integer   |
| `entity_has_subscriptions`       | Binary flag (`1` or `0`) if the description mentions subscriptions.           | Integer   |
| `entity_has_refunds`             | Binary flag (`1` or `0`) if the description mentions refunds.                   | Integer   |
| `entity_has_P2P`                 | Binary flag (`1` or `0`) if the description mentions peer-to-peer transfers.    | Integer   |
| `entity_has_digital_goods`       | Binary flag (`1` or `0`) if the description mentions digital goods.             | Integer   |

## D. One-Hot Encoded Features

The original `merchant_category` and `transaction_country` text features were converted into multiple binary indicator columns. Each new column (e.g., `merchant_category_Electronics`, `transaction_country_US`) has a value of `1` if the transaction belongs to that category, and `0` otherwise.

## E. TF-IDF Features

The original `transaction_description` text feature was converted into 100 numerical features using `TfidfVectorizer`. Each column (e.g., `tfidf_urgent`, `tfidf_purchase`) represents the Term Frequency-Inverse Document Frequency score of a specific word, indicating its importance in the description relative to the entire dataset.

```

---

## 3. Exploratory Data Analysis (EDA)
EDA was performed in `notebooks/exploratory_data_analysis.ipynb`. The key goals were to understand the distribution of fraudulent vs. non-fraudulent transactions, analyze the relationships between different features, and identify initial patterns. This step was crucial for informing the feature engineering process. *(Note: Visualizations from the notebook should be embedded here in the final report.)*

---

## 4. Feature Engineering
Feature engineering was a multi-step process designed to convert raw data, especially text, into numerical features suitable for machine learning.

- **LLM-driven Features**: The `llm_feature_engineering.py` script and `feature_extraction.ipynb` notebook were used to process the transaction descriptions using a large language model. This generated features like `transaction_purpose_encoded`, `entity_has_crypto`, and `suspicious_word_score`.
- **TF-IDF Vectorization**: Term Frequency-Inverse Document Frequency (TF-IDF) was used to convert the text descriptions into a numerical format, capturing the importance of words.
- **Behavioral Features**: Features like `consistency_score` and `transaction_frequency` were engineered to model user behavior over time.
- **Final Cleaning**: The `final_cleaning_and_preparation.py` script performed the last cleaning steps to create the `final_ml_ready_dataset.csv`.

---

## 5. Modelling
A variety of models were trained to identify the most effective approach for this fraud detection problem. Each model was saved as a script in the `src/` directory and documented in a corresponding notebook in `notebooks/`.

The models evaluated include:
- **Decision Tree (`Model_Baseline`)**: An interpretable, rule-based model.
- **Random Forest (`Model_A`)**: An ensemble of decision trees for improved robustness.
- **XGBoost (`Model_B`)**: A powerful gradient boosting framework.
- **Logistic Regression (`Model_C`)**: A linear model establishing a baseline.
- **K-Nearest Neighbors (`Model_D`)**: A non-parametric, instance-based model.
- **Neural Network (`Model_E`)**: A multi-layer perceptron for capturing non-linear patterns.

---

## 6. Results

The performance of each model was evaluated on a held-out test set. The key metrics are summarized below:

| model                | description                                                                                                                                                             |   accuracy |   precision |   recall |   f1_score |   roc_auc |
|:---------------------|:------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------:|------------:|---------:|-----------:|----------:|
| Decision Tree        | A standard Decision Tree classifier. Hyperparameters: {'random_state': 42, 'class_weight': 'balanced', 'max_depth': 10}                                                 |   0.963104 |    0.781818 | 0.716667 |   0.747826 |  0.846912 |
| Random Forest        | An ensemble of Decision Trees. Hyperparameters: {'n_estimators': 100, 'random_state': 42, 'class_weight': 'balanced', 'max_depth': 10}                                  |   0.968193 |    0.888889 | 0.666667 |   0.761905 |  0.963866 |
| XGBoost              | A gradient boosting framework. Hyperparameters: {'random_state': 42, 'eval_metric': 'logloss', 'scale_pos_weight': np.float64(12.092857142857143), 'n_estimators': 100} |   0.969466 |    0.875    | 0.7      |   0.777778 |  0.965657 |
| Logistic Regression  | A linear model for binary classification. Includes StandardScaler. Hyperparameters: {'random_state': 42, 'class_weight': 'balanced', 'solver': 'liblinear'}             |   0.952926 |    0.676923 | 0.733333 |   0.704    |  0.887029 |
| K-Nearest Neighbors  | A non-parametric method using distance to classify. Hyperparameters: {'n_neighbors': 5}                                                                                 |   0.956743 |    1        | 0.433333 |   0.604651 |  0.854132 |
| Neural Network (MLP) | A Multi-Layer Perceptron classifier. Hyperparameters: {'hidden_layer_sizes': (100, 50), 'random_state': 42, 'max_iter': 500, 'early_stopping': True}                    |   0.966921 |    0.904762 | 0.633333 |   0.745098 |  0.950436 |

---

## 7. Business Insights & Model Summaries

## 8. AI Usage Disclosure
This project was developed with the assistance of an AI coding agent (Gemini). The AI's role included:
- **Code Generation**: Writing boilerplate code for scripts and notebooks.
- **Debugging**: Identifying and fixing errors, such as the `ModuleNotFoundError` in notebooks.
- **Analysis & Summarization**: Generating textual summaries and insights for the model evaluation sections based on typical model performance characteristics.
- **Report Generation**: Creating the Python script to automate the generation of this report.

## 9. GitHub Contribution Summary
To generate a summary of contributions for this project, you can use the following Git command. This command lists the number of commits per author, providing an overview of the development activity.

```bash
# To get a short summary of commits per author
git shortlog -s -n

# To get a more detailed log, you can run:
git log --pretty=format:"%h - %an, %ar : %s"
```

This concludes the final report.
